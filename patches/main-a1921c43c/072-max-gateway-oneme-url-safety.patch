diff --git a/tests/tools/test_url_safety.py b/tests/tools/test_url_safety.py
index 12b5b92ac5742dc6025918894afe16f881f1c6af..b4585fee02b5809e62ea15fb048a9e321fe78f52 100644
--- a/tests/tools/test_url_safety.py
+++ b/tests/tools/test_url_safety.py
@@ -169,6 +169,22 @@ class TestIsSafeUrl:
         ]):
             assert is_safe_url("https://multimedia.nt.qq.com.cn/download?id=123") is True
 
+    def test_max_oneme_image_hostname_allowed_with_benchmark_ip(self):
+        with patch("socket.getaddrinfo", return_value=[
+            (2, 1, 6, "", ("198.18.17.74", 0)),
+        ]):
+            assert is_safe_url("https://i.oneme.ru/i?r=photo-token") is True
+
+    @pytest.mark.parametrize("url", [
+        "https://sub.i.oneme.ru/i?r=photo-token",
+        "http://i.oneme.ru/i?r=photo-token",
+    ])
+    def test_max_oneme_image_hostname_exception_is_exact_https_match(self, url):
+        with patch("socket.getaddrinfo", return_value=[
+            (2, 1, 6, "", ("198.18.17.74", 0)),
+        ]):
+            assert is_safe_url(url) is False
+
     def test_qq_multimedia_hostname_exception_is_exact_match(self):
         with patch("socket.getaddrinfo", return_value=[
             (2, 1, 6, "", ("198.18.0.23", 0)),
diff --git a/tools/url_safety.py b/tools/url_safety.py
index 860d4d9dfa40259d0a3f5645faeb8fe58a1b8374..4d763a1f4f962cdc78cc0ffd6a233e7239a03753 100644
--- a/tools/url_safety.py
+++ b/tools/url_safety.py
@@ -57,10 +57,12 @@ _ALWAYS_BLOCKED_NETWORKS = (
 )
 
 # Exact HTTPS hostnames allowed to resolve to private/benchmark-space IPs.
-# This is intentionally narrow: QQ media downloads can legitimately resolve
-# to 198.18.0.0/15 behind local proxy/benchmark infrastructure.
+# This is intentionally narrow: some messenger CDN media hosts legitimately
+# resolve to 198.18.0.0/15 behind local proxy/benchmark infrastructure.
+# Keep this as exact hostnames only; do not allow wildcard subdomains.
 _TRUSTED_PRIVATE_IP_HOSTS = frozenset({
     "multimedia.nt.qq.com.cn",
+    "i.oneme.ru",
 })
 
 # 100.64.0.0/10 (CGNAT / Shared Address Space, RFC 6598) is NOT covered by
