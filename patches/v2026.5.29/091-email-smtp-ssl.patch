From 4f75edb6d99a46d213f16badd7d9a3c969ffd3a9 Mon Sep 17 00:00:00 2001
From: Anry777 <Anry777@users.noreply.github.com>
Date: Fri, 29 May 2026 07:54:23 +0300
Subject: [PATCH] patchkit: reanchor email smtp ssl for v2026.5.29

---
 gateway/platforms/email.py | 36 ++++++++++++++++++++++++++++--------
 tools/send_message_tool.py |  9 +++++++--
 2 files changed, 35 insertions(+), 10 deletions(-)

diff --git a/gateway/platforms/email.py b/gateway/platforms/email.py
index 0fffb82d0..a5456353a 100644
--- a/gateway/platforms/email.py
+++ b/gateway/platforms/email.py
@@ -316,8 +316,13 @@ class EmailAdapter(BasePlatformAdapter):
 
         try:
             # Test SMTP connection
-            smtp = smtplib.SMTP(self._smtp_host, self._smtp_port, timeout=30)
-            smtp.starttls(context=ssl.create_default_context())
+            smtp_security = os.getenv("EMAIL_SMTP_SECURITY", "").strip().lower()
+            use_ssl = smtp_security in {"ssl", "tls", "ssl/tls", "smtps"} or (not smtp_security and self._smtp_port == 465)
+            if use_ssl:
+                smtp = smtplib.SMTP_SSL(self._smtp_host, self._smtp_port, timeout=30, context=ssl.create_default_context())
+            else:
+                smtp = smtplib.SMTP(self._smtp_host, self._smtp_port, timeout=30)
+                smtp.starttls(context=ssl.create_default_context())
             smtp.login(self._address, self._password)
             smtp.quit()
             logger.info("[Email] SMTP connection test passed.")
@@ -548,9 +553,14 @@ class EmailAdapter(BasePlatformAdapter):
 
         msg.attach(MIMEText(body, "plain", "utf-8"))
 
-        smtp = smtplib.SMTP(self._smtp_host, self._smtp_port, timeout=30)
-        try:
+        smtp_security = os.getenv("EMAIL_SMTP_SECURITY", "").strip().lower()
+        use_ssl = smtp_security in {"ssl", "tls", "ssl/tls", "smtps"} or (not smtp_security and self._smtp_port == 465)
+        if use_ssl:
+            smtp = smtplib.SMTP_SSL(self._smtp_host, self._smtp_port, timeout=30, context=ssl.create_default_context())
+        else:
+            smtp = smtplib.SMTP(self._smtp_host, self._smtp_port, timeout=30)
             smtp.starttls(context=ssl.create_default_context())
+        try:
             smtp.login(self._address, self._password)
             smtp.send_message(msg)
         finally:
@@ -670,9 +680,14 @@ class EmailAdapter(BasePlatformAdapter):
             except Exception as e:
                 logger.warning("[Email] Failed to attach %s: %s", file_path, e)
 
-        smtp = smtplib.SMTP(self._smtp_host, self._smtp_port, timeout=30)
-        try:
+        smtp_security = os.getenv("EMAIL_SMTP_SECURITY", "").strip().lower()
+        use_ssl = smtp_security in {"ssl", "tls", "ssl/tls", "smtps"} or (not smtp_security and self._smtp_port == 465)
+        if use_ssl:
+            smtp = smtplib.SMTP_SSL(self._smtp_host, self._smtp_port, timeout=30, context=ssl.create_default_context())
+        else:
+            smtp = smtplib.SMTP(self._smtp_host, self._smtp_port, timeout=30)
             smtp.starttls(context=ssl.create_default_context())
+        try:
             smtp.login(self._address, self._password)
             smtp.send_message(msg)
         finally:
@@ -749,9 +764,14 @@ class EmailAdapter(BasePlatformAdapter):
             part.add_header("Content-Disposition", f"attachment; filename={fname}")
             msg.attach(part)
 
-        smtp = smtplib.SMTP(self._smtp_host, self._smtp_port, timeout=30)
-        try:
+        smtp_security = os.getenv("EMAIL_SMTP_SECURITY", "").strip().lower()
+        use_ssl = smtp_security in {"ssl", "tls", "ssl/tls", "smtps"} or (not smtp_security and self._smtp_port == 465)
+        if use_ssl:
+            smtp = smtplib.SMTP_SSL(self._smtp_host, self._smtp_port, timeout=30, context=ssl.create_default_context())
+        else:
+            smtp = smtplib.SMTP(self._smtp_host, self._smtp_port, timeout=30)
             smtp.starttls(context=ssl.create_default_context())
+        try:
             smtp.login(self._address, self._password)
             smtp.send_message(msg)
         finally:
diff --git a/tools/send_message_tool.py b/tools/send_message_tool.py
index dab83b854..d638ec98c 100644
--- a/tools/send_message_tool.py
+++ b/tools/send_message_tool.py
@@ -1290,8 +1290,13 @@ async def _send_email(extra, chat_id, message):
         msg["Subject"] = "Hermes Agent"
         msg["Date"] = formatdate(localtime=True)
 
-        server = smtplib.SMTP(smtp_host, smtp_port)
-        server.starttls(context=ssl.create_default_context())
+        smtp_security = (extra.get("smtp_security") or os.getenv("EMAIL_SMTP_SECURITY", "")).strip().lower()
+        use_ssl = smtp_security in {"ssl", "tls", "ssl/tls", "smtps"} or (not smtp_security and smtp_port == 465)
+        if use_ssl:
+            server = smtplib.SMTP_SSL(smtp_host, smtp_port, context=ssl.create_default_context())
+        else:
+            server = smtplib.SMTP(smtp_host, smtp_port)
+            server.starttls(context=ssl.create_default_context())
         server.login(address, password)
         server.send_message(msg)
         server.quit()
-- 
2.39.5

